# KiosPay Frontend

Dokumentasi utama sudah dipindahkan ke README root repository:

- `../README.md`

Ringkasan frontend:

- Stack: Next.js (App Router), TypeScript, Tailwind, Framer Motion
- Env template: `.env.example`
- Route guard: `src/proxy.ts`
- Start production: `npm run start`
